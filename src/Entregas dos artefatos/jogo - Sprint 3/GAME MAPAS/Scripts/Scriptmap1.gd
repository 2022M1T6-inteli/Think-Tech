extends Node2D

var dialog


# Called when the node enters the scene tree for the first time.
func _ready():
	
	#Checkpoint.lastPosition=Vector2(47,80)
	$fundo.play()
	#Checkpoint.check=2
	


"""
func _on_Area2D_body_entered(body):
	if body.name == "Player":
		Checkpoint.check=2
"""
